# generated by verwalter {{ verwalter_version }}

image-dir: /var/lib/lithos/images/{{ role }}
readonly-paths:
  /secrets: /etc/secrets/{{ role }}
writable-paths:
  /storage: /var/lib/lithos/storage/{{ role }}
allow-users: [1]
allow-groups: [1]
