# Generated by verwalter {{ verwalter_version }}

{{#if daemons}}
    {{#each daemons}}
    {{ @key }}:
        kind: Daemon
        image: {{ image }}
        config: {{ config }}
        instances: {{ instances }}
    {{/each}}
{{/if}}

{{#if commands}}
    {{#each commands}}
    {{ @key }}:
        kind: Command
        image: {{ image }}
        config: {{ config }}

    {{/each}}
{{/if}}
